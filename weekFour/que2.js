// Take an array with 5 colors. Create another array by adding two more colors to it.

const colors = ['red', 'blue', 'green', 'yellow', 'grey'];
const newColors = [...colors, 'black', 'white'];

console.log(newColors);
